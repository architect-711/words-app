# The WordsApp backend

## Documentation
[Here](https://app.weeek.net/ws/688666/kb/11)

## How to run the app
1. You need the `postgres` installed with database and it's credentials, like in the `.env` file
2. `jdk-21` installed
3. If you have OSX/Linux, then go the project `backend` folder, then run `source ../.env` and `./gradlew clean build && ls build/libs`