logging:
    level:
        org:
            springframework:
                security: DEBUG
        root: ERROR
        edu.architect_711.words.startup: INFO

# Variables from .env file. For Docker only
spring:
    datasource:
        url: ${SPRING_DATASOURCE_URL}
        username: ${POSTGRES_ROOT_USERNAME}
        password: ${POSTGRES_ROOT_PASSWORD}